import styled from "styled-components";
import Cart from "./cart";
export default styled(Cart)`
  .shopping-cart {
    max-width: 60rem;
    margin: 0 auto;
    margin-top: 3rem;
    font-size: 3rem;
  }
`;
